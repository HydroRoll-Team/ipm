on:
  push:
  workflow_dispatch:

jobs:
  test-api:
    name: test commit
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: set up pdm
        run: pip install pdm
      
      - name: install test deps
        run: |
          pdm install -dG test
        
      - name: test api
        run: |
          cd test
          python -m test-api.py